# Shipping Harness v1.9.0 — Engineering Infrastructure and Maintainability

## 1. 문제

v1.8.2 시점의 리포 평가(2026-09-12) 결과, 제품 개념·안전 경계·릴리즈 거버넌스는 9점대였으나 반복 가능한 엔지니어링 인프라가 낮았다.

| 항목 | 점수 | 근거 |
|---|---:|---|
| 툴링·CI | 4 | CI 없음. ESLint·Prettier·tsc 없음. 자체 lint는 탭·후행공백·문법만, typecheck는 import 해석만 검사 |
| 유지보수성 | 5 | 단일 기여자. `release:verify`가 `npm run` 21개 체인. doc audit 필수 파일 47개 하드코딩. 일회성 pilot 스크립트 잔존. 예외를 삼키는 `catch {}` 24곳 |
| 문서 | 6 | README에 버전 섹션 23개 누적. MCP.md 뒤에 버전별 섹션 누적. README 헤더 v1.8.1 vs package 1.8.2 불일치 |
| 배포·사용성 | 6 | 절대경로 tgz 설치. 설치 경로 3가지 병존 |
| 코드 구조 | 7 | `callShippingTool` 487줄, `refineScopeProposal` 203줄, CLI if 체인 302줄. export 함수 JSDoc 247/366 |
| 테스트 | 8 | 커버리지 측정 없음. 릴리즈 급 슈트가 `npm test` 밖에 분산 |

## 2. 목표

동작(권한 정책·MCP 툴 9개·스키마·상태 기계·출력 형식)을 바꾸지 않고, 위 여섯 항목을 모두 8점 이상으로 올린다.

## 3. 고정 원칙

- 런타임 `dependencies`는 계속 0개다. 도구는 `devDependencies`에만 들어간다.
- `.shipping/` 런타임 파일은 `release prepare` 외의 방법으로 수정하지 않는다.
- MCP 툴 이름·입력 스키마·출력 필드, CLI 명령·옵션·종료 코드, 모든 `schemas/v1` 스키마는 바이트 단위로 동일하게 유지한다. 리팩터링은 기존 테스트 193개와 릴리즈 슈트가 안전망이다.
- 사람 정지·계약·증거·Judge 순의 권한 순서와 안전 경계(`docs/HANDOVER.md`)는 손대지 않는다.
- 새 파일은 ESM `.mjs`, `node:` 접두 builtin, JSDoc 타입을 따른다.
- 커밋은 단계별로 하나 이상 남기되, `lock`·`close`·태그는 수행하지 않는다. 그것은 소유자의 승인 행위다.

## 4. 구현 단계

### Phase A — 릴리즈 준비와 정적 분석 도구

1. `node bin/shipping-harness.mjs release prepare --version 1.9.0 --goal "Repeatable engineering infrastructure: CI, static analysis, coverage, documentation restructure, and maintainability refactors without changing Shipping authority, MCP surface, or schemas"` 를 실행해 DRAFT를 만든다. `package.json` version을 `1.9.0`으로 올린다.
2. `.shipping/contract.yaml`의 acceptance를 아래로 교체한다(ID는 `AC-190nn`).
   - AC-19001 `npm run check` (lint, typecheck, build, test)
   - AC-19002 `npm run lint:eslint`
   - AC-19003 `npm run typecheck:tsc`
   - AC-19004 `npm run test:coverage` (임계값 포함)
   - AC-19005 `npm run release:verify`
   - AC-19006 `git diff --check`
3. `devDependencies`에 `eslint`(최신 10.x), `@eslint/js`, `globals`, `typescript`(최신 5.x), `@types/node`를 추가하고 `package-lock.json`을 커밋한다. `--ignore-scripts` 설치 관행을 유지한다.
4. `eslint.config.mjs`(flat config)를 추가한다. 대상은 `src/ bin/ scripts/ test/ packages/`. `@eslint/js` recommended + `no-unused-vars`(args: after-used, `_` 접두 허용), `no-empty`(빈 catch는 주석이 있을 때만 허용), `eqeqeq`, `prefer-const`, `no-var`, `no-implicit-globals`. 기존 자체 lint(탭·후행공백)는 `scripts/lint.mjs`에 그대로 두고 `npm run lint`가 둘 다 실행하게 한다.
5. `tsconfig.json`을 추가한다. `allowJs`, `checkJs`, `noEmit`, `strict: false`에서 시작하되 `noImplicitAny: false`, `strictNullChecks: true`, `module: NodeNext`, `target: ES2022`, `types: ["node"]`, include는 `src/**/*.mjs bin/**/*.mjs packages/**/*.mjs`. `npm run typecheck:tsc`는 `tsc -p tsconfig.json`. 기존 `scripts/typecheck.mjs`는 유지하고 `npm run typecheck`가 둘 다 실행하게 한다.
6. ESLint와 tsc가 보고하는 모든 오류를 고친다. 실제 버그로 판단되는 것은 테스트를 먼저 추가한다. tsc 오류가 300개를 넘으면 `strictNullChecks`를 끄고 그 사실을 이 문서 6장에 기록한다.
7. `scripts/security-check.mjs`는 `dependencies`만 검사하므로 그대로 두되, `devDependencies`가 `bin/`·`src/`·`packages/`에서 import되지 않는지 검사하는 규칙을 추가한다.
8. `.nvmrc`(`22`)와 `.editorconfig`(LF, 2 spaces, no trailing whitespace, final newline)를 추가한다.
9. `npm test`에 `--experimental-test-coverage`를 붙인 `npm run test:coverage`를 추가하고, `scripts/run-tests.mjs`가 `--coverage` 인자를 받으면 라인 커버리지 임계값(초기값: 현재 측정치에서 2%p 낮춘 값, 정수)을 `--test-coverage-lines`로 강제하게 한다. 측정치를 이 문서 6장에 기록한다.
10. `../dev-wiki/decisions/2026-09-12-shipping-harness-정적분석-CI-도입.md`에 ADR을 쓴다(커밋하지 않는다). 내용: 런타임 의존성 0을 유지하면서 devDependencies로 ESLint·tsc를 도입하는 결정, 대안(자체 스크립트 유지), 결과.

### Phase B — 코드 구조와 유지보수성

1. `src/mcp/tools.mjs`의 `callShippingTool`을 툴 이름 → 핸들러 맵으로 분해한다. 핸들러는 `src/mcp/handlers/<tool>.mjs` 또는 `src/mcp/handlers.mjs` 하나에 두고, `SHIPPING_TOOLS` 정의는 그대로 둔다. 공통 전처리(인자 검증·권한 확인·오류 정규화)는 한 곳에 남긴다. 목표: 어떤 함수도 80줄을 넘지 않는다.
2. `src/cli.mjs`의 if 체인을 `command → handler` 테이블로 바꾼다. `main(argv)` 시그니처, `handleCliError`, 종료 코드, `renderHelp` 출력은 동일해야 한다.
3. `src/core/proposals.mjs`의 `refineScopeProposal`·`createScopeProposal`·`approveScopeProposal`을 검증/계산/기록 단계로 분해한다. 80줄 목표.
4. `catch {}` 및 `catch (…) {}` 24곳을 전수 조사해 (a) 의도적 무시면 한 줄 주석으로 이유를 남기고, (b) 오류를 알아야 하는 곳이면 `ShippingError`로 변환하거나 진단 필드에 기록한다. 결과 표를 이 문서 6장에 남긴다.
5. `release:verify`의 `npm run` 21개 체인을 `scripts/release-verify.mjs` 하나로 옮긴다. 각 단계를 이름·명령·소요 시간·결과로 기록하고, 독립 단계는 CPU 수만큼 병렬 실행하며, 끝에 실패 요약을 출력한다. `npm run release:verify`는 이 스크립트를 호출한다. `--list`로 단계 목록만 출력하는 옵션을 둔다.
6. `scripts/` 중 특정 버전 작업에만 쓰였고 어떤 npm script·테스트·doc audit에서도 참조되지 않는 파일(`amend-v09-packaging-scope.mjs`, `omo-amend-v07-contract.mjs`, `v1-amend-plugin-test-scope.mjs`, `omo-v0.7-gate.mjs`, `evoharvest-intelligence-pilot.mjs` 등 실제 grep으로 확인)을 `scripts/archive/`로 옮기고 `scripts/archive/README.md`에 각 파일이 어느 버전에 쓰였는지 적는다. `package.json` `files`와 lint 대상에서 제외한다.
7. `scripts/v1-doc-audit.mjs`의 하드코딩 `required` 목록을 (a) 항상 필요한 핵심 문서 목록(README, BACKLOG, HANDOVER 등)과 (b) `docs/planning/*.md`·`schemas/v1/*.schema.json`·`schemas/v1/examples/*.example.json`·`docs/reports/*.json` 디렉터리 스캔으로 나눈다. 스캔 규칙: 모든 스키마에 예제가 있어야 하고, 계획 문서 번호가 연속이어야 한다. 기존에 검사하던 필드 리포트 검증 로직은 유지한다.
8. export 함수 중 JSDoc이 없는 119개에 `@param`·`@returns`를 채운다. tsc가 이를 타입으로 쓴다.

### Phase C — 문서 구조

1. `CHANGELOG.md`를 새로 만들고 README의 `## Version boundaries` 아래 버전 섹션 23개와 `docs/MCP.md` 하단의 버전별 섹션(v0.9, v1.7.0, v1.8.1 등)을 여기로 옮긴다. Keep a Changelog 형식, 최신 버전이 위.
2. README를 다음 구조로 재작성한다(120줄 이하): 한 문장 정의 → 왜 필요한가(현재 Why 축약) → 핵심 보장 → 설치(한 가지 권장 경로 + 나머지는 링크) → 5분 사용(CLI와 MCP 각각) → 릴리즈 흐름 그림 → 안전 경계 → 문서 지도(문서를 역할별로 묶은 표: 시작/운영/설계/계획/보고) → 라이선스. 버전 표기는 한 곳만 두고 `package.json`과 같아야 한다.
3. `scripts/v1-doc-audit.mjs`에 규칙을 추가한다. README와 `docs/HANDOVER.md`에 나타나는 `Current release train`·`v1.x.y` 헤더 버전이 `package.json` version과 같아야 한다. CHANGELOG의 최상단 항목 버전도 같아야 한다.
4. `docs/MCP.md`를 버전 누적 없이 현재 동작 기준으로 재구성한다. 버전 이력은 CHANGELOG로 보낸다. 툴 9개의 이름·목적·입력·전제 상태·결과를 표 하나로 정리한다.
5. 설치 문서를 `docs/operations/INSTALL-UPGRADE-ROLLBACK.md` 하나로 모은다. 권장 경로는 "태그된 커밋에서 `npm pack` 후 `npm install --global --prefix ~/.local <tgz>`"이며, GitHub Release 첨부 tgz 설치를 향후 경로로 적는다(실제 Release 생성은 이 버전 범위 밖).
6. `AGENTS.md`에 "버전 하나를 처음부터 끝까지 닫는 실습 절차" 섹션을 추가한다. `release prepare`부터 태그까지 실제 명령과 각 단계에서 확인할 파일을 적는다.
7. 한/영 혼용은 다음 규칙으로 정리한다. 코드 주석·스키마·CLI 출력은 영어. `docs/planning`은 한국어 유지. README·MCP·HANDOVER는 영어 유지, `docs/BEGINNER-QUICKSTART-KO.md`가 한국어 진입점. 이 규칙을 AGENTS.md에 적는다.

### Phase D — CI

1. `.github/workflows/ci.yml`: push와 pull_request에서 Node 22와 24 매트릭스로 `npm ci --ignore-scripts`, `npm run check`, `npm run lint:eslint`, `npm run typecheck:tsc`, `npm run test:coverage`, `npm run security`, `npm run license:check`를 실행한다. (CI 체크아웃에서는 diff가 비어 `git diff --check`가 무의미하므로 제외한다.)
2. `.github/workflows/release-verify.yml`: `workflow_dispatch`와 태그 push(`v*`)에서 Node 22로 `npm run release:verify`를 실행하고 `dist/BUILD-MANIFEST.json`을 아티팩트로 올린다.
3. 두 워크플로 모두 `permissions: contents: read`, 타임아웃 20분, 캐시는 `actions/setup-node`의 npm 캐시만 쓴다. 외부 액션은 `actions/checkout`과 `actions/setup-node`만 허용하고 메이저 버전으로 고정한다.
4. README 상단에 CI 배지를 넣는다. 원격 저장소 URL은 `git remote get-url origin`으로 확인하고, 원격이 없으면 배지를 생략하고 그 사실을 6장에 적는다.

## 5. 완료조건

- `npm run check`, `npm run lint:eslint`, `npm run typecheck:tsc`, `npm run test:coverage`, `npm run release:verify`, `git diff --check`가 모두 통과한다.
- `src/`·`packages/`에 80줄을 넘는 함수가 없다(`scripts/lint.mjs`에 함수 길이 검사를 추가해 강제한다. 기존 위반이 남으면 허용 목록에 넣고 6장에 적는다).
- MCP 툴 9개의 `SHIPPING_TOOLS` JSON, `schemas/v1/**`, `renderHelp()` 출력이 v1.8.2와 바이트 단위로 같다(테스트로 고정한다: `test/stable/surface-freeze.test.mjs`가 해시를 비교한다).
- README 120줄 이하, 버전 표기가 `package.json`과 일치하고 doc audit이 이를 검사한다.
- `.shipping/state.json`은 `DRAFT`·release `1.9.0`이며 lock·close·태그는 수행되지 않았다.
- 런타임 `dependencies`는 0개다.

## 6. 실행 기록

### Phase A

- **devDependencies 설치**: `eslint@^10.10.0`, `@eslint/js@^10.0.1`, `globals@^17.12.0`,
  `typescript@^5.9.3`, `@types/node@^22.20.2`를 `npm install --save-dev --ignore-scripts`로
  설치했다. `package.json` `dependencies` 필드는 여전히 없음(0개).
- **ESLint**: `eslint.config.mjs`(flat config)를 신설했다. 대상 `src/ bin/ scripts/ test/
  packages/`, ignore `node_modules/ dist/ scripts/archive/`. 초기 오류 **31건** → 수정 후
  **0건**(`npm run lint:eslint`). 오류는 대부분 미사용 import/변수, `let x = <초기값>`
  뒤 항상 재할당되는 무의미한 초기값(`no-useless-assignment`), 이유 주석 없는 빈 catch,
  불필요한 정규식 이스케이프였다. 실질적 동작 변경 없이 죽은 코드 제거·주석 추가·
  이스케이프 정리로 해결했다(`src/core/proposals.mjs`의 제어문자 제거 정규식만
  `eslint-disable-next-line no-control-regex` 예외 처리 — 의도된 보안 sanitize이므로).
- **tsc**: `tsconfig.json` 신설(`allowJs`+`checkJs`+`noEmit`, `strict:false`,
  `noImplicitAny:false`, **`strictNullChecks:true` 유지** — 초기 오류가 300개 미만이라
  끄지 않았다). 초기 오류 **237건** → 수정 후 **0건**(`npm run typecheck:tsc`).
  - 가장 큰 단일 원인(약 60여 건)은 `invariant(condition, code, message)` 계열 어서션
    헬퍼(`src/core/errors.mjs`, `packages/internal-remote/policy.mjs`,
    `packages/omp-main-harness/io.mjs`, `packages/stable-control/errors.mjs`)에 JSDoc
    `@returns {asserts condition}`가 없어 `possibly null/undefined` 오류가 연쇄된 것이었다.
    네 곳 모두에 어서션 시그니처를 추가해 대부분 해소했다.
  - 나머지는 개별 함수의 JSDoc `@param`/`@returns` 누락 또는 부정확(예: `Record<string,
    any>`를 스프레드하면 인덱스 시그니처가 사라지는 TS의 알려진 동작, `let x = []`
    형태의 빈 배열/객체 리터럴이 `never[]`/`{}`로 굳어지는 추론, `new Error()`에 임의
    속성을 붙일 때 타입 캐스트 누락)였다. 모두 타입 주석만 보강했고 런타임 동작은
    바꾸지 않았다. 유일하게 로직을 만졌던 곳(`packages/shipping-plugin/installer/cli.mjs`의
    `--host` 처리)은 되돌렸다 — 처음에는 알 수 없는 host 값을 `'generic'`으로 조용히
    강제하도록 고쳤으나, 이는 `plan.mjs`가 런타임에 `ERR_PLUGIN_HOST`로 명시적으로
    거부하던 것과 동작이 달라져 되돌리고 타입만 `string`으로 넓혔다.
  - `npm test`(193개) 전부 통과 상태를 유지했다(회귀 없음).
- **devDependency 런타임 유입 방지**: `scripts/security-check.mjs`에 `bin/`·`src/`·
  `packages/`의 모든 import 지정자를 스캔해 `package.json` `devDependencies` 이름과
  겹치면 실패시키는 규칙을 추가했다.
- **`.nvmrc`**(`22`), **`.editorconfig`**(LF, space 2, 최종 개행, 후행공백 제거;
  `.md`는 후행공백 규칙 예외)를 신설했다.
- **커버리지**: `node --test --experimental-test-coverage`로 전체 스위트(`test/unit`
  `test/integration` `test/adapter` `test/mcp` `test/adversarial`)를 측정한 라인
  커버리지는 **86.17%**(정수 86). 임계값은 측정치 −2%p인 **84**로 설정해
  `scripts/run-tests.mjs`가 `--coverage` 인자를 받으면
  `--test-coverage-lines=84`를 강제한다. `npm run test:coverage`로 실행하며 현재
  통과한다(exit 0).
- **함수 길이/카탈로그 정리(Phase B 항목)**: Phase A 범위 밖. 착수하지 않음.
- **CI 배지 / 원격 저장소**: Phase D 범위. Phase A에서는 다루지 않음.
- **ADR**: `../dev-wiki/decisions/2026-09-12-shipping-harness-정적분석-CI-도입.md`에
  작성(dev-wiki 리포에는 커밋하지 않음 — 지시에 따름).

### Phase B

**안전망 먼저**: 리팩터 전에 `test/stable/surface-freeze.test.mjs`를 추가해 `SHIPPING_TOOLS`
JSON, `renderHelp()` 출력, `schemas/v1/**` 내용의 SHA-256을 v1.8.2 종료 시점 값에
고정했다(각각 `f4cf418c…`, `cf161627…`, `563fa876…`). 스키마 해시는 `schemas/v1/<상대경로>:sha256(내용)`
줄을 바이트 정렬해 개행으로 합친 뒤 다시 SHA-256한 값이다. 이후 1~3단계 리팩터
내내 이 테스트를 계속 통과시켰다.

**1. `src/mcp/tools.mjs` 분해**: `SHIPPING_TOOLS` 정의는 그대로 두고, 487줄짜리
`callShippingTool`을 이름→핸들러 맵(`TOOL_HANDLERS`, `src/mcp/handlers.mjs`)으로
바꿨다. 공통 전처리(`objectArguments`/`rejectUnknownKeys`/`requiredString`/`complete`)는
`src/mcp/validation.mjs` 하나로, `ADAPTERS`/`emptyObjectSchema`는 `src/mcp/constants.mjs`로
옮겨 `tools.mjs`↔`handlers.mjs` 순환 참조를 피했다. `shipping_start`·`shipping_refine`·
`shipping_approve_scope`는 각각 `validate*`/`build*`/`compose*` 헬퍼로 더 쪼갰다.

**2. `src/cli.mjs` 분해**: 302줄 if 체인을 `COMMANDS` 이름→핸들러 테이블로 바꿨다.
`main(argv)` 시그니처, `handleCliError`, 종료 코드, `renderHelp()` 출력은 동일하다.
`issue` 명령이 `add` 이외 하위 명령을 받으면 여전히 `Unknown command: issue`를 낸다.

**3. `src/core/proposals.mjs` 분해**: `createScopeProposal`(123줄) → `loadProposalContext`
/`reuseMatchingProposal`/`composeProposalDecision`/`buildProposalDiagnostics`/
`assembleProposal`/`persistProposal`. `refineScopeProposal`(198줄) → `validateRefineRequest`
/`buildRefineEvidence`/`composeRefineDecision`/`mergeRefineIntelligence`/`buildRefineCandidate`
/`persistRefinedProposal`. `approveScopeProposal`(119줄) → `authorizeApproval`
/`validateApprovableProposal`/`lockApprovedContract`/`persistApprovalArtifacts`
/`recordApprovalState`/`recordApprovalLedger`. `Record<string, any>`를 객체 리터럴에
스프레드하면 인덱스 시그니처가 사라지는 tsc 동작 때문에 두 곳(`candidate`, `approved`)에
`/** @type {Record<string, any>} */`를 붙였다.

**4. `catch {}` 전수 조사**: `src/`·`packages/`에서 매개변수 없는 `catch {` 24곳을 grep으로
확인했다(`grep -rnE "catch\s*\{" --include="*.mjs" src/ packages/ | grep -v "catch (error)\|..."`
로 재현 가능). 표:

| 파일:라인 | 결정 | 이유 |
|---|---|---|
| `src/core/fs.mjs:23` | (a) 의도적 무시 | `exists()` 존재 확인: 어떤 접근 오류든 "없음"과 동일 |
| `src/core/decision-evidence.mjs:64` | (a) 의도적 무시 | 손상된 릴리즈 영수증 1건이 전체 이력 스캔을 막지 않도록 `UNREADABLE`로 기록 |
| `src/core/baseline.mjs:24` | (a) 의도적 무시 | git이 인용한 경로의 JSON 디코딩 실패 시 따옴표만 벗기는 최선-노력 대체 |
| `src/core/hooks.mjs:46` | (b) ShippingError로 변환 | `JSON.stringify` 실패를 `ERR_HOOK_PAYLOAD`로 변환하고 원인 메시지를 `details.reason`에 추가 |
| `src/mcp/stdio.mjs:28` | (a) 의도적 무시(기존 주석 있음) | 큐 초과 시 요청 id 파싱 실패해도 오버플로 응답은 계속 보냄 |
| `src/core/autopilot.mjs:270` | (b) 진단 필드로 기록 | 잠긴 계약 없음은 `reason: 'NO_CURRENT_LOCKED_CONTRACT'`로 이미 보고됨 |
| `src/mcp/protocol.mjs:266` | (b) McpProtocolError로 변환 | JSON.parse 원본 메시지가 공격자 입력을 반영할 수 있어 표준 Parse error 코드만 노출 |
| `src/core/decision-ledger.mjs:94` | (b) Error로 변환 | 손상된 원장 줄은 무시하지 않고 `ERR_DECISION_LEDGER_PARSE`로 닫힘 실패 |
| `packages/internal-omo-bridge/client.mjs:27` | (b) ShippingError로 변환 | 비-JSON stdout을 `ERR_OMO_RUNTIME_OUTPUT`으로 변환, stderr 노출 |
| `packages/omp-main-harness/field-smoke.mjs:18` | (a) 의도적 무시 | Git 저장소가 아니면 베이스라인 없음(`null`)으로 보고 |
| `src/core/process.mjs:21` | (a) 의도적 무시 | 프로세스 그룹 kill 실패 시 자식 프로세스 직접 kill로 대체 |
| `src/core/process.mjs:29` | (a) 의도적 무시 | 위와 동일한 SIGKILL 최종 강제 종료 대체 |
| `packages/omp-main-harness/io.mjs:41` | (a) 의도적 무시 | `exists()` 존재 확인(위 fs.mjs와 동일 패턴) |
| `packages/omp-main-harness/io.mjs:72` | (b) invariant로 변환 | `access(X_OK)` 실패를 `ERR_OMP_EXECUTABLE`로 변환 |
| `packages/omp-main-harness/io.mjs:146` | (a) 의도적 무시(기존 주석 있음) | PATH 탐색 중 후보 실패는 계속 탐색 |
| `packages/stable-control/schema-registry.mjs:162` | (a) 의도적 무시 | JSON Schema `anyOf` 조합자: 분기 거부는 정상 제어 흐름 |
| `packages/stable-control/schema-registry.mjs:168` | (a) 의도적 무시 | 위와 동일한 `oneOf` 조합자 |
| `packages/omp-main-harness/package.mjs:43` | (a) 의도적 무시 | 설치된 버전 조회 실패 시 `null`(doctor/status 프로브) |
| `packages/omp-main-harness/package.mjs:53` | (a) 의도적 무시 | 실행 가능 여부 프로브 실패 시 `false` |
| `src/core/project-analysis.mjs:162` | (a) 의도적 무시 | 사람이 읽는 분석용 최선-노력 디렉터리 목록; 실패 시 빈 배열 |
| `packages/internal-remote/backup.mjs:183` | (a) 의도적 무시(기존 주석 있음) | `state.json` 없음/손상은 "활성 상태 없음"으로 처리 |
| `packages/internal-remote/backup.mjs:210` | (a) 의도적 무시(기존 주석 있음) | 복원 실패 시 최선-노력 롤백; 원래 오류가 우선 |
| `packages/omp-main-harness/doctor.mjs:121` | (a) 의도적 무시 | 실행 가능 여부 doctor 체크 실패 시 `false` |
| `packages/stable-control/compatibility.mjs:69` | (a) 의도적 무시 | git 버전 조회 실패는 `'unavailable'`로 보고(진단 정보 자체가 목적) |

의도적 무시 17곳, 이미 오류로 변환/진단 필드로 기록되어 있던 7곳 — 모두 이유 주석을 추가했고,
`hooks.mjs`는 원인 메시지를 details에 추가로 보강했다. 동작 변경 없음(193개 테스트 유지).

**5. `scripts/release-verify.mjs`**: `package.json`의 `npm run` 16개 체인(`check`,
`test:plugin`, `test:omo-bridge`, `test:remote`, `test:stable`, `security`,
`security:inventory`, `license:check`, `verify:docs`, `smoke:release-train`,
`smoke:autopilot`, `smoke:autopilot:field`, `smoke:goal-discovery`, `smoke:goal-charter`,
`test:goal-charter:field`, `smoke:goal-charter:field`)를 `os.cpus().length` 크기 워커
풀에서 병렬 실행하는 스크립트로 옮겼다(각 단계가 이미 자체 임시 픽스처를 쓰거나
읽기 전용 검사라 안전하다). 이름·소요시간·결과 표와, `node:test` 요약의
`ℹ skipped N`을 파싱한 스킵 카운트, 스킵된 단계마다 눈에 띄는 `WARNING` 줄을 출력한다.
실패 단계가 없을 때만 exit 0. `--list`로 단계 이름만 출력한다. 최종 실행 요약(발췌):

```
release:verify summary
------------------------------------------------------------------------
PASS check                          13.5s
PASS test:plugin                     0.9s
PASS test:omo-bridge                 0.9s (5 skipped)
PASS test:remote                     0.7s
PASS test:stable                     0.5s
PASS security                        0.5s
PASS security:inventory              0.5s
PASS license:check                   0.2s
PASS verify:docs                     0.3s
PASS smoke:release-train             0.6s
PASS smoke:autopilot                 0.9s
PASS smoke:autopilot:field           1.4s
PASS smoke:goal-discovery            1.0s
PASS smoke:goal-charter              0.9s
PASS test:goal-charter:field         1.3s
PASS smoke:goal-charter:field        2.1s
------------------------------------------------------------------------

WARNING: the following steps had skipped tests:
  WARNING: test:omo-bridge skipped 5 test(s) - private OMO runtime is not installed at the pinned path

All 16 steps passed. See WARNING above for skipped tests.
```

부수적으로 `docs/reports/v1-security-inventory.json`이 Phase A부터 이미 stale해 있던 것을
발견해 `--record`로 재생성했다(파일 내용 해시만 갱신, 보안 경계 필드는 무변경). Phase B의
이후 커밋(catch 주석, JSDoc 추가)이 같은 파일들을 다시 건드릴 때마다 재생성을 반복했다.

**6. `scripts/archive/`**: 실제 grep으로 참조를 확인한 뒤 6개 파일을 옮겼다 —
`amend-v09-packaging-scope.mjs`(v0.9.0, `.shipping/amendments/AMEND-0.9.0-001.json`에만
역사적으로 언급됨), `omo-amend-v07-contract.mjs`(v0.7.0, 참조 없음),
`v1-amend-plugin-test-scope.mjs`(플러그인 범위 확장, 참조 없음),
`omo-v0.7-gate.mjs`(v0.7.0 AC 게이트, `test:omo-bridge`/`smoke:*`로 대체됨),
`evoharvest-intelligence-pilot.mjs`(v1.3.0/v1.4.0, `docs/TRACEABILITY.md` 등 과거
계획 문서에서만 언급), `plain-brief-pilot.mjs`(v1.4.0, `docs/TRACEABILITY.md`에서만
언급). `scripts/archive/README.md`에 각 파일의 버전과 이유를 적었다. 반대로
`omo-pilot.mjs`·`mcp-smoke.mjs`·`remote-acceptance.mjs`·`team-dag-entry-gate.mjs`는
문서화된 운영 도구이거나(`docs/internal-runtime/README.md`, `docs/MCP.md`,
`docs/internal-remote/README.md`) 실제 테스트가 import하므로(`test/team-dag/entry-gate.test.mjs`)
보관하지 않았다. `scripts/lint.mjs`가 `scripts/archive/**`를 제외하도록 고쳤다
(`eslint.config.mjs`는 이미 제외하고 있었다). `package.json` `files`에는 애초에 6개 중
어느 것도 없었다.

**7. `scripts/v1-doc-audit.mjs` 재구성**: 47개 하드코딩 경로를 (a) 항상 필요한 핵심
문서 18개와 (b) 두 개의 디렉터리 스캔으로 나눴다 — `auditPlanningSequence`
(`docs/planning/*.md`가 `00-`부터 빈틈없이 번호가 매겨져야 함)와
`auditSchemaExamplePairs`(모든 `schemas/v1/*.schema.json`에 짝이 되는
`examples/*.example.json`이 있어야 하고 그 역도 성립). 기존 필드 리포트 검증
로직(plain-brief/release-train/autopilot-field/goal-charter-field)은 그대로 뒀다.
수동 검증: `schemas/v1/examples/goals.example.json`과 `plain-brief.example.json`을
각각 지웠을 때 감사가 실패(exit 1)함을 확인 후 복원, `docs/planning/33-....md`를
`34-...`로 리네임했을 때 "docs/planning numbering has a gap: expected 33, found 34"로
실패함을 확인 후 복원(diff로 내용 동일함 확인).

**8. JSDoc 보강**: export 중 JSDoc이 없는 116개(계획 문서의 119개 추정치와 근접;
Phase A의 tsc 작업 중 이미 상당수가 채워졌다)에 `@param`/`@returns`를 채웠다.
기존에 암묵적 `any`였던 매개변수이므로 대부분 `{*}`(=any)로 타입을 명시했고,
`async` 함수는 tsc의 TS1064 요구사항에 따라 `@returns {Promise<*>}`로 감쌌다.
클래스 7개(`RateLimiter`, `InternalRemoteGateway`, `NotificationStore`, `ReplayStore`,
`StableEventLog`, `McpLineClient`, `ShippingRemoteAdapter`)에는 한 줄 클래스 설명만
추가했다. `npm run typecheck:tsc`는 0건 유지.

**9. `test/stable/surface-freeze.test.mjs`**: 위 "안전망 먼저" 참고.

**10. 함수 길이 검사**: `scripts/function-length.mjs`가 문자열/템플릿 리터럴/주석/
(최선-노력) 정규식 리터럴을 공백으로 마스킹한 뒤, `=>` 또는 `if/for/while/switch/catch/with`가
아닌 토큰이 앞에 오는 `)` 뒤의 `{`를 "함수 시작"으로 분류해 중괄호 깊이로 길이를 잰다.
`scripts/lint.mjs`가 `src/`·`packages/`의 모든 `.mjs`에 대해 이를 실행해 80줄 초과를
실패시킨다(스캔 자체를 임시 87줄 함수로 검증). 기존 위반 13건은 아래 허용 목록에
기록했다(모두 이 Phase 이전부터 있던, 하나의 감사된 순차 흐름·디스패치 테이블·단일
객체 조립 함수):

| 파일:줄 | 함수 | 이유 |
|---|---|---|
| `src/core/decision-package.mjs:165` | `composeDefaultDecision` | 증거로부터 결정 객체 하나를 필드별로 조립; 분리하면 상호 연관된 중간값을 헬퍼 사이에 전달하는 것 뿐 |
| `src/core/evidence.mjs:20` | `runAcceptance` | 잠긴 계약의 인수 명령을 예산/증거 계정과 함께 하나의 감사된 순서로 실행 |
| `src/core/hooks.mjs:54` | `decideStop` | 하나의 유한 라이프사이클 결정 테이블; 분기들이 요청/어댑터 컨텍스트를 공유 |
| `src/core/plain-brief.mjs:230` | `buildBriefFactGraph` | plain-brief 스키마가 요구하는 많은 리터럴 필드를 가진 하나의 유한 사실 그래프 객체 조립 |
| `src/core/process.mjs:43` | `runBoundedCommand` | 하나의 원자적 spawn/timeout/출력상한/정리 시퀀스; 분리하면 정리 로직이 실패 경로에서 분리될 위험 |
| `src/core/project-analysis.mjs:244` | `inspectWorkspace` | 하나의 파일시스템 검사 패스와 그로부터의 단일 보고서 조립 |
| `src/mcp/protocol.mjs:119` | `createMcpProtocol` | 실제 본문은 `handle()` 메서드(아래) 하나뿐인 작은 팩토리 |
| `src/mcp/protocol.mjs:138` | `handle` | 9개 shipping_* 툴과 프로토콜 메서드의 JSON-RPC 디스패치 테이블 |
| `src/mcp/stdio.mjs:7` | `startStdioServer` | stdin/stdout의 유한 큐/배압 상태 기계를 한 곳에 설계상 배치(모듈 주석 참고) |
| `src/mcp/user-view.mjs:33` | `buildUserStatusView` | 상태 필드로부터 하나의 유한 사용자 뷰 객체 조립 |
| `packages/internal-omo-bridge/bridge.mjs:43` | `executeShippingPrivateOmo` | probe/promote/fallback 권한 결정을 감사 가능한 단일 시퀀스로 유지해야 함 |
| `packages/omp-main-harness/doctor.mjs:14` | `doctorOmpMainHarness` | 독립적인 doctor 체크들을 하나의 보고서로 순차 집계 |
| `packages/omp-main-harness/install.mjs:80` | `installOmpMainHarness` | 롤백 상태를 보호하는 단계들과 분리할 수 없는 하나의 순차 install 파이프라인 |

**11. `packages/internal-remote/https-server.mjs`**: 미니파이된 두 줄을 `readBody`/
`statusForError`/`handleRequest`(요청당 로직)/`createInternalHttpsServer` 함수로
가독성 있게 재구성했다. 클로저 변수였던 `active` 카운터는 `handleRequest`에 명시적으로
전달되는 `{ value }` 객체로 바꿨다(동일한 공유 가변 상태). `test/remote/https.test.mjs`로
검증(2/2 통과, `test:remote` 17/17 통과).

**12. 비공개 OMO 런타임 테스트**: `config/upstreams/omo-pin.json`이 가리키는
`/home/jm/orca/projects/shipping-harness-omo-runtime`이 이 머신에는 없다(저장소는
`.../archive/`로 이동됨). pin 파일은 지시대로 손대지 않았다. `test/omo/helpers.mjs`에
`privateOmoRuntimeAvailability()`를 추가해 `<runtimeRoot>/runtime-manifest.json` 존재
여부로 판정하고, 실제 런타임 바이너리를 호출하는(픽스처가 의도적으로 깨뜨린 경로가
아닌) 5개 테스트에 `node:test`의 `{ skip: '...' }` 옵션을 적용했다. 런타임을 쓰지 않는
3개 테스트(사람 정지, 승인된 폴백, 폴백 없는 BLOCKED)는 그대로 실행된다.
`test:omo-bridge`는 이제 3 pass / 0 fail / 5 skipped로 exit 0. 5번 항목의
`release-verify.mjs`가 이 스킵을 요약과 WARNING 줄에 반영한다.

**완료하지 못한 항목**: 없음. Phase B의 1~12번(원안 1~8 + 추가 9~12)을 모두 완료했다.
함수 길이 허용 목록의 13건은 "완료 못함"이 아니라 계획이 명시적으로 허용한 예외 처리다.

### Phase C

- **`CHANGELOG.md` 신설**: Keep a Changelog 형식, 최신 버전이 위. README의 `## Version
  boundaries` 아래 버전 섹션 23개(v0.1.0~v1.7.0, Safety boundary 뒤에 있던 v1.7.0 포함)와
  `docs/MCP.md` 하단의 버전별 누적 섹션(v1.3.1/v1.4.0/v1.5.0/v1.6.0 — "Local installation"
  본문에 인라인으로 섞여 있던 것 포함, v1 verification, v0.9 internal remote gateway,
  v1.6.1 field hardening, v1.7.0 bounded goal discovery, v1.8.1 Goal Direction field
  boundary)을 모두 옮겼다. `git log`와 `docs/planning/30-...`/`32-...`를 근거로 README·
  MCP.md 어디에도 없던 v1.8.0(Goal Charter)과 v1.8.2(Plain Brief Budget Hotfix) 항목도
  추가해 버전 이력에 공백이 없게 했다. 1.9.0 항목은 "Unreleased" 상태로 계획 문서 1~2장,
  6장 요약을 근거로 작성했고 종료일 없이 "in progress"로 표시했다. 총 **버전 항목 27개**
  (`grep -c '^## \[' CHANGELOG.md`): 0.1.0~1.9.0의 모든 패치 버전(1.0.1/1.0.2/1.1.1/1.1.2/
  1.3.1/1.6.1/1.8.1/1.8.2 등)을 개별 항목으로 나눴고, README/MCP.md에는 없었지만 실제
  닫힌 v1.8.0과 v1.8.2를 추가로 채워 넣었다.
- **README 재작성**: 303줄 → **91줄**(120줄 이하 충족). 구조: 제목/`**Version:** 1.9.0`
  → 한 줄 정의 → Why → Core guarantees → Install(권장 경로 1가지 + INSTALL-UPGRADE-ROLLBACK
  링크) → 5분 사용(CLI/MCP 각각) → Release flow → Safety boundary → Documentation map
  (역할별 표: Start/Operate/Design/Plans/Reports/Research) → License. 스테일했던
  `Current release train`/`Authority` 헤더 줄을 삭제했다. 버전 표기는 `**Version:**
  1.9.0` 한 곳뿐이며 `package.json`과 일치한다. 기존 README가 링크하던 문서 25개
  전부가 Documentation map 표에서 여전히 도달 가능하다(직접 링크 유지, `CHANGELOG.md`도
  Plans 행에 추가).
- **doc audit 버전 일치 규칙**: `scripts/v1-doc-audit.mjs`에 `auditVersionMentions()`를
  추가했다. README의 `**Version:** X.Y.Z` 줄, `docs/HANDOVER.md`의 `**Current version:**
  X.Y.Z` 줄(맨 위에 새로 추가; 기존 `## vX.Y.Z handover` 형태의 과거 이력 헤딩들은
  검사 대상이 아님), CHANGELOG 최상단 `## [X.Y.Z]` 항목이 모두 `package.json` version과
  같아야 한다. 수동 검증: README의 버전 줄을 `1.9.9`로 바꿔 `npm run verify:docs`가
  `README.md version line is 1.9.9, package.json is 1.9.0`로 실패(exit 1)함을 확인한 뒤
  원복, 이후 `verify:docs`가 다시 통과함을 확인했다.
- **`docs/MCP.md` 재구성**: 버전 누적 없이 현재 동작 기준으로 다시 썼다. 9개 툴 표는
  `src/mcp/tools.mjs`의 `SHIPPING_TOOLS`(이름·목적·`inputSchema.required`)와
  `src/mcp/handlers.mjs`(전제 상태·결과)를 실제로 읽고 파생했다 — 추측하지 않았다.
  설치·승인 흐름·프로토콜 호환성·경계·트러블슈팅 섹션은 유지했고, 버전 이력은
  CHANGELOG로 보냈다.
- **설치 문서 통합**: `docs/operations/INSTALL-UPGRADE-ROLLBACK.md`는 이미 Phase 이전에
  존재했다(clean install/upgrade/rollback 전체 절차 보유). README와 `docs/MCP.md`는
  각각 권장 설치 경로 한 가지만 인라인으로 보여주고 업그레이드/롤백 등 나머지는 그
  문서로 링크하도록 고쳤다 — 별도로 옮길 절차 문서가 새로 필요하지 않았다(이미
  단일화되어 있었음).
- **`AGENTS.md`**: "Version cycle walkthrough" 섹션을 추가해 `release prepare`부터
  태그까지 12단계 각각의 정확한 명령과 그 다음에 확인할 파일을 적었다. 이번 세션에서
  실제로 겪은 `ERR_RELEASE_DRIFT` 문제(`release prepare`가 `.shipping/` 밖 변경을
  `state.closedGitSha`와 비교하므로 close 커밋 이후 비-`.shipping/` 파일을 건드리는
  커밋보다 먼저 실행해야 함)와 해당 우회법(close 커밋을 detached worktree로 체크아웃해
  거기서 `release prepare` 실행 후 `.shipping/`만 복사)을 그대로 기록했다. 이어서
  "Language rule" 섹션을 추가했다(코드 주석·스키마·CLI 출력은 영어, `docs/planning`은
  한국어, README/MCP/HANDOVER는 영어이고 `docs/BEGINNER-QUICKSTART-KO.md`가 한국어
  진입점).
- **완료하지 못한 항목**: 없음. Phase C의 1~7번을 모두 완료했다.

### Phase D

- **원격 저장소 확인**: `git remote get-url origin` → `https://github.com/whelp99-code/shipping-harness.git`.
  원격이 존재하므로 워크플로는 실제로 실행되며, README에 CI 배지를 넣었다.
- **`.github/workflows/ci.yml`**: `push`/`pull_request` 트리거, Node `22`/`24` 매트릭스,
  `actions/checkout@v4` + `actions/setup-node@v4`(npm 캐시)만 사용, `permissions:
  contents: read`, `timeout-minutes: 20`. `npm ci --ignore-scripts` 뒤 `npm run check`,
  `npm run lint:eslint`, `npm run typecheck:tsc`, `npm run test:coverage`, `npm run
  security`, `npm run license:check`, `git diff --check`를 각각 별도 `run:` 단계로
  실행해 실패한 단계 이름이 그대로 드러나게 했다.
- **`.github/workflows/release-verify.yml`**: `workflow_dispatch`와 `v*` 태그 push
  트리거, Node `22`, 동일한 checkout/setup-node/권한/타임아웃 제약. `npm ci
  --ignore-scripts` 뒤 `npm run release:verify`(이미 `check` 단계에서 빌드를 포함하므로
  `dist/BUILD-MANIFEST.json`이 이미 생성됨)를 실행하고 `actions/upload-artifact@v4`로
  그 파일을 업로드한다.
- **범위 제한**: `.github/workflows/`에 이 두 파일만 추가했다. Dependabot, CODEOWNERS,
  이슈 템플릿은 추가하지 않았다.
- **YAML 검증**: 이 환경에 Python `pyyaml`이 설치되어 있어 `yaml.safe_load()`로 두
  워크플로 파일을 직접 파싱해 문법 오류가 없음을 확인했다(GitHub Actions 관례대로
  `on:` 키가 YAML 1.1에서 boolean `True`로 로드되는 것은 정상이며 GitHub은 이를
  트리거 키로 특별 취급한다). 추가로 `git diff --check`가 깨끗함과 탭 문자가 없음을
  확인했다.
- **완료하지 못한 항목**: 없음. Phase D의 1~4번을 모두 완료했다.

### Fix pass: JSDoc types

Phase B에서 export 116개에 JSDoc을 붙일 때 남겨 둔 자리표시자 타입을 전부 실제
타입으로 교체했다.

- **수량**: 교체 전 `grep -rn "{\*}\|Promise<\*>" src packages` 263줄(`@param {*}`
  약 155개 + `@returns {*}` / `Promise<*>`), 교체 후 **0줄**. `npm run
  typecheck:tsc`(`tsc --checkJs`, `strictNullChecks`)는 시작과 끝 모두 오류 0으로
  유지했고, 모듈 단위로 교체할 때마다 재실행했다.
- **대상 모듈**: `src/mcp/resources.mjs`, `src/cli/output.mjs`,
  `src/core/{redaction,autopilot,autopilot-policy,release-train,decision-ledger,
  project-analysis,project-intelligence,goal-discovery,plain-brief,goal-charter}.mjs`,
  `packages/{internal-remote,stable-control,omp-main-harness,internal-omo-bridge,
  shipping-plugin}/…` 총 30개 파일.
- **추가한 `@typedef`**: `OmpMainPaths`, `FieldFixture`, `RunCommandOptions`,
  `FileReceipt`(omp-main-harness), `RawRemoteConfig`/`RawRemoteActor`/
  `RawRemoteProject`/`RemoteActor`/`RemoteProject`/`RemoteConfig`(internal-remote
  identity), `RemoteRequest`, `ApprovalReceipt`, `BackupFile`/`BackupBundle`,
  `StableSchemaDescriptor`/`JsonSchemaNode`, `MigratedArtifact`,
  `ShippingStatusLike`(health), `AutopilotPolicy`, `AutopilotState`/
  `AutopilotEvent`/`AutopilotDecision`/`AutopilotRecordResult`/
  `AutopilotMutationReceipt`/`CloseResult`, `ReleaseTrain`/`ReleaseTrainRelease`/
  `ReleaseTrainBinding`/`ReleaseTrainEnvelope`, `DecisionLedgerEvent`,
  `CandidateCommand`/`TopLevelEntry`/`ProjectAnalysis`/`AcceptanceCriterion`,
  `AcceptanceCommandMetadata`, `GoalDiscovery`와 그 하위 6종,
  `PlainBriefInput`/`PlainBrief`/`ActionEnvelope`/`BriefFactGraph` 등 8종,
  `GoalCharter`/`GoalCharterBinding`. 모듈 안에서 반복되던 긴 인라인 객체 타입은
  모두 이 typedef로 모았고, 모듈 간에는 `import('./x.mjs').TypeName` 로 참조했다.
- **`unknown` 사용처**: 런타임에서 직접 검증하는 입력에만 썼다 —
  `policy.rejectArbitraryExecution`/`boundedString`, `io.validateAbsoluteRoot`,
  `crypto.stable`/`hmac`(서명 대상 임의 문서), `parseJsonOutput`(외부 명령 stdout
  JSON), `validateStableArtifact`/`validateStableDocument`.
  `parseJsonOutput`이 `unknown`이 되면서 유일한 소비자인
  `omp-main-harness/config.mjs`의 호출부에 `Record<string, unknown>` JSDoc 캐스트를
  한 줄 추가했다.
- **실제 버그**: 없었다. 타입을 정확히 붙이면서 드러난 불일치는 모두 호출부/피호출부
  JSDoc이 실제 동작보다 좁게 적혀 있던 경우였고, 코드가 아니라 JSDoc을 실제 동작에
  맞췄다. 대표적으로 `internal-remote/backup.mjs`의 `createBackup`은
  `extraEvidenceFiles`의 `source`가 falsy면 건너뛰도록 이미 구현돼 있는데 JSDoc은
  `source: string`만 허용하고 있어 `source: string|null`로 넓혔다(`gateway.mjs`가
  `runtimePinFiles`의 `null` 항목을 그대로 넘긴다).
- **런타임 무변경**: 실행 코드는 바꾸지 않았다. 타입 때문에 손댄 곳은 JSDoc 주석과
  `/** @type {…} */` 캐스트, 그리고 `internal-omo-bridge/bridge.mjs`의
  `configuredFallback` 반복 배열에 붙인 `/** @type {const} */` 뿐이다. 함수 길이
  허용 목록(`scripts/lint.mjs`)의 `startLine` 두 건은 JSDoc 줄이 늘어난 만큼
  갱신했다(`bridge.mjs` 43→47, `plain-brief.mjs` 230→246).
- **검증**: `npm run check`, `npm run lint:eslint`, `npm run typecheck:tsc`,
  `npm run test:coverage`, `npm run test:stable`(surface-freeze 포함),
  `git diff --check` 모두 통과.
