# Shipping Harness v1.13.0 — Dirty-Tree Friction

## 1. 문제

실제 사용(jmai-os-pack, 2026-09-17)에서 PATCH 릴리스 하나에 사용자 결정이 여덟 번 필요했다. 원인을 픽스처로 재현해 세 가지 결함을 확인했다.

### 1.1 더러운 트리에서 제안을 만들면 나중에 버린다

| 커밋 시점 | 하네스 반응 | 결과 |
|---|---|---|
| 승인 **전** 선행 커밋 | `ERR_PROPOSAL_STALE: Repository HEAD changed after proposal creation` | 제안 폐기. 재스캔 후 사용자가 승인 브리프를 다시 검토 |
| lock **후** 선행 커밋 | 오류 없음, SHIPPABLE, 차단 0건 | 선행 커밋이 릴리스 diff에 흡수됨 |

하네스는 더러운 트리를 받아 제안을 만들어 놓고, 사용자가 정리하면 그 제안을 버린다. 정리를 미루면 증거가 오염된다. 정리 시점을 사용자가 맞춰야 하는 구조 자체가 문제다.

### 1.2 lock 이후 커밋이 릴리스에 조용히 흡수된다

제안이 만드는 `scope.paths.include`는 분석기가 찾은 소스 루트 전부다. 픽스처에서는 `README.md`, `package.json`, `scripts/**`, `src/**`, `test/**`로 사실상 리포 전체였다. lock 이후 목표와 무관한 파일을 커밋해도 범위 이탈로 잡히지 않고 통과하며, receipt는 승인 범위에 없던 작업이 섞인 트리를 증명한다.

### 1.3 목표가 지목한 경로가 범위에서 빠진다

```
목표: "Implement hello() in a new file src/index.mjs so the existing test suite passes"
제안된 scope.paths.include: ["README.md","README.rst","README.txt","package.json","test/**"]
```

베이스라인에 `src/` 디렉터리가 없으면 분석기가 찾지 못하고, 목표 문장에 경로가 적혀 있어도 읽지 않는다. 그대로 구현하면 첫 verify가 `Unapproved scope drift: src/index.mjs`로 차단된다. 신규 프로젝트와 새 영역을 만드는 계획 단계가 모두 해당한다.

## 2. 목표

셋을 모두 없앤다. 사용자는 커밋 시점을 결정하지 않고, 승인 후 커밋은 증거로 남으며, 목표가 지목한 경로는 범위에 들어간다.

## 3. 고정 원칙

- 하네스가 만드는 git 변경은 **로컬·되돌릴 수 있는 커밋 하나**뿐이다. push·tag·amend·rebase·`reset --hard`·브랜치 전환은 하지 않는다. 안전 경계(`docs/HANDOVER.md`)는 그대로다.
- 추적되지 않은 파일을 무조건 담지 않는다. `src/adapters/artifacts.mjs`의 `BLOCKED_BASENAMES`(자격증명류)를 재사용해 걸러내고, 하나라도 걸리면 **커밋 자체를 거부**하고 파일명을 보고한다. 조용히 빼고 진행하지 않는다.
- `.gitignore`가 무시하는 파일과 `.shipping/**`는 어떤 경우에도 스테이징하지 않는다.
- 범위를 넓히는 근거는 사용자가 승인한 목표 문장뿐이다. 넓힌 사실과 이유를 승인 브리프에 드러낸다.
- 1.2의 판정은 차단이 아니라 증거다. 커밋이 목표와 관련 있는지는 결정론적으로 판단할 수 없으므로, 기록해 사람이 보게 한다.
- MCP 툴은 9개를 유지한다. 입력은 optional 가산만. 표면 동결 해시 갱신과 사유 기록.

## 4. 구현 단계

### Phase A — 베이스라인 자동 커밋 (1.1)

1. `src/core/baseline-commit.mjs`(신규):
   - `collectBaselineChanges(root)` → `{ trackedModified, untracked, shippingSkipped }`. `git ls-files --others --exclude-standard`로 untracked를 구해 무시된 파일이 절대 들어오지 않게 한다. `.shipping/**`는 제외.
   - `screenUntracked(paths)` → `BLOCKED_BASENAMES` 위반, 개별 8 MiB 초과, 총 200개 초과를 검사. 위반 시 `ShippingError('ERR_BASELINE_UNSAFE_UNTRACKED', …, { path, reason })`.
   - `commitBaseline(root)` → `git add -u`, 통과한 untracked만 `git add -- <path>`, 고정 메시지로 커밋. 반환 `{ sha, files, untrackedIncluded, undo }`.
   - 커밋 메시지(고정, 작성자 귀속 줄 없음 — 사용자 자신의 작업이다):
     ```
     chore: commit working tree before the shipping-harness proposal

     Recorded by shipping-harness <version> so the release baseline is one commit.
     Undo: git reset --soft HEAD~1
     ```
   - 실패 시 `ERR_BASELINE_COMMIT_FAILED`에 git stderr를 담아 던지고, 인덱스를 `git reset -q`로 되돌려 스테이징 잔여물을 남기지 않는다.
2. `shipping_start` 입력에 optional `commitBaseline`(boolean, **기본 true**). false면 기존 동작.
3. `createScopeProposal` 진입 직후, 저장소 분석 **전에** 실행한다. 변경이 없으면 무동작. 커밋했으면 그 SHA가 제안의 `baselineSha`가 된다.
4. 원장에 `baseline.autocommitted` 이벤트(sha, 파일 수, untracked 수)를 남긴다. 상태 전이는 아니다.
5. 응답: `structuredContent.baselineCommit = { sha, filesCommitted, untrackedIncluded, undo }` 또는 `null`. 텍스트 맨 위에 한 줄로 알린다.
6. **이미 통과하는 인수 조건 경고.** 자동 커밋이 구현 작업을 베이스라인에 넣으면 그 릴리스는 아무것도 증명하지 못한 채 닫힐 수 있다. 자동 커밋이 발생했을 때만 v1.12.1의 preflight를 돌려, 필수 인수 조건이 전부 이미 통과하면 `BASELINE_ALREADY_PASSING` 진단과 승인 안내 문장을 붙인다. 차단하지 않는다. preflight 자체가 실패하면 진단 없이 계속한다.

### Phase B — 승인 후 커밋 기록 (1.2)

1. `closeRelease`가 receipt에 `postLockCommits`를 쓴다: `baselineSha`(= lock 시점)부터 `closedGitSha`까지의 커밋을 `{ sha, subject, paths }`로. 유계: 커밋 50개, 커밋당 경로 20개, subject 120자. 초과 시 `truncated: n`.
2. `releaseStatus`에 `commitsSinceLock`(정수)을 더해 `status` 텍스트·MCP·릴리스 보고서에 노출한다. LOCKED 이후 상태에서만 계산한다.
3. 차단하지 않는다. `release.schema.json`에 가산 optional.

### Phase C — 목표가 지목한 경로 (1.3)

1. `src/core/goal-paths.mjs`(신규, 순수): `extractGoalPaths(goalText)` → 경로처럼 보이는 토큰을 뽑는다. 규칙:
   - `/`를 포함하거나 알려진 소스 확장자로 끝나는 토큰만.
   - 절대 경로, `..` 포함, `~` 시작은 버린다.
   - 유계: 최대 8개, 토큰당 200자.
2. `scopePathsForGoal(goalText, scopePaths, root)` → 각 경로를 글로브로 바꾼다. 파일의 상위 디렉터리 + `/**`. 상위가 리포 루트면 글로브 대신 그 파일 경로 그대로. 이미 include에 덮이면 추가하지 않는다.
3. 거부 규칙: `scope.paths.exclude`(`.git/**`, `node_modules/**`, `dist/**` 등)에 걸리거나 `.shipping/`으로 시작하면 추가하지 않고 `GOAL_PATH_REFUSED` 진단을 남긴다. `assertContainedPath`로 리포 밖을 막는다(파일이 실제로 없어도 된다).
4. 추가한 것은 `GOAL_PATH_ADDED: <glob> (goal named "<token>")` 진단으로 승인 브리프에 드러낸다.
5. 적용 지점은 제안이 계약의 `scope.paths.include`를 만드는 곳 하나다. 계획 단계(v1.12.0)의 `scopeInclude`가 있는 경우에도 같은 목표 문장에 대해 동일하게 적용한다.

### Phase D — 테스트

- `test/unit/baseline-commit.test.mjs`: 깨끗한 트리 → 무동작; 추적 수정만 → 커밋되고 SHA 반환; 무시된 파일과 `.shipping/` 제외; 안전한 untracked 포함.
- `test/adversarial/baseline-commit-attacks.test.mjs`: untracked `.env`/`id_rsa`/`credentials.json` → `ERR_BASELINE_UNSAFE_UNTRACKED`로 **커밋이 일어나지 않고** `git status --porcelain`이 호출 전과 바이트 단위로 같고 `git diff --cached`가 비어 있음; 8 MiB 초과; 200개 초과; `.gitignore` 대상 미포함.
- `test/integration/baseline-autocommit-flow.test.mjs`: 더러운 트리 → `shipping_start` → 자동 커밋 → 승인까지 `ERR_PROPOSAL_STALE` 없이 도달하고 `baselineSha`가 자동 커밋 SHA와 같음; `commitBaseline: false`면 기존 동작; 자동 커밋으로 전 AC가 통과하면 `BASELINE_ALREADY_PASSING`.
- `test/unit/post-lock-commits.test.mjs`: lock 후 커밋 2개 → receipt `postLockCommits` 길이 2와 경로; 커밋 없으면 빈 배열; 상한 초과 시 `truncated`.
- `test/unit/goal-scope-paths.test.mjs`: `src/index.mjs` → `src/**` 추가; 루트 파일 → 그 파일만; 이미 덮인 경로는 미추가; 최대 8개.
- `test/adversarial/goal-scope-attacks.test.mjs`: 목표 문장의 `../../etc/passwd`, `/etc/passwd`, `.git/config`, `node_modules/x`, `.shipping/state.json` → 전부 미추가 + `GOAL_PATH_REFUSED`; 1.3 시나리오 재현(빈 `src/`에서 목표가 `src/index.mjs`를 지목 → 구현이 scope drift로 차단되지 않음).

### Phase E — 문서

`docs/MCP.md`(shipping_start 입력·출력·되돌리기, 진단 코드), `docs/BEGINNER-QUICKSTART-KO.md`(더러운 트리로 시작해도 된다는 한 단락), `docs/HANDOVER.md`(하네스가 로컬 커밋을 만드는 유일한 지점과 경계, `postLockCommits`의 의미), `packages/shipping-plugin/skill/SKILL.md`, `CHANGELOG`.

## 5. 완료조건

- 더러운 트리에서 `shipping_start` → 승인까지 재스캔 없이 도달한다.
- 자격증명류 untracked 파일이 있으면 커밋이 일어나지 않고 오류가 파일명을 지목하며 트리가 그대로다.
- `.gitignore` 대상과 `.shipping/`은 어떤 경우에도 커밋되지 않는다.
- lock 이후 커밋이 receipt에 남고 `status`가 개수를 보여준다.
- 베이스라인에 없는 디렉터리를 목표가 지목해도 구현이 차단되지 않으며, 넓힌 범위가 진단으로 드러난다.
- 목표 문장을 통한 경로 탈출이 불가능하다.
- `commitBaseline: false`로 기존 동작을 그대로 얻는다.
- `npm run release:verify` 통과, 표면 동결 해시 갱신 사유 기록.

## 6. 실행 기록

### Phase A — 베이스라인 자동 커밋

`src/core/baseline-commit.mjs`(신규)가 하네스가 git 히스토리를 쓰는 유일한 지점이다.

- `collectBaselineChanges(root)` → `{ headSha, trackedModified, stagedAdditions, untracked, shippingSkipped }`. 계획의 3개 키에 `stagedAdditions`를 더했다. 사용자가 손으로 `git add` 한 신규 파일은 `git ls-files --others` 에 더 이상 나오지 않으므로, `git diff --name-status --no-renames HEAD` 의 `A` 항목을 따로 뽑아 untracked 와 **같은** 안전 검사를 받게 한다. 이 구분이 없으면 미리 스테이징된 `.env` 가 검사를 통과해 커밋된다.
- `screenUntracked(root, paths)`. 계획 서명은 `screenUntracked(paths)` 였으나 개별 파일 크기를 재려면 루트가 필요해 인자를 하나 늘렸다. 검사 항목은 `BLOCKED_BASENAMES`(`src/adapters/artifacts.mjs` 에서 새로 export), 같은 모듈의 `BLOCKED_SEGMENTS`(`.git`/`.shipping`/`.ssh`/`.aws`/`.gnupg` — 계획의 "자격증명류" 취지를 디렉터리까지 확장), 개별 8 MiB 초과, 총 200개 초과, 일반 파일이 아님, 읽을 수 없음. 하나라도 걸리면 `ShippingError('ERR_BASELINE_UNSAFE_UNTRACKED', …, { path, reason })` 를 던지고 **아무것도 스테이징하지 않는다**.
- `commitBaseline(root)`. `git reset -q` 로 인덱스를 HEAD 로 되돌린 뒤(작업 트리는 그대로) 열거된 경로만 100개 단위로 `git add -- <path>` 하고 `git -c commit.gpgsign=false commit --no-verify -m <고정 메시지>` 로 커밋한다. `git add -A`/`git add .` 는 쓰지 않는다. `--no-verify` 와 서명 비활성화는 의도적이다: `shipping_start` 는 "저장소 코드를 실행하지 않고 분석한다"고 약속하므로, 저장소 훅을 실행하거나 서명 프롬프트에서 멈추면 그 약속과 베이스라인의 결정성이 함께 깨진다. 실패하면 `git reset -q` 로 인덱스를 되돌리고 `ERR_BASELINE_COMMIT_FAILED` 에 git stderr 를 담아 던진다(스택 트레이스 아님). 커밋이 없는 저장소는 같은 코드로 `ERR_BASELINE_COMMIT_FAILED` 가 된다.
- 커밋 메시지는 계획 그대로이며 귀속 줄이 없다. 커밋되는 내용은 사용자 자신의 작업이다.

```
chore: commit working tree before the shipping-harness proposal

Recorded by shipping-harness 1.13.0 so the release baseline is one commit.
Undo: git reset --soft HEAD~1
```

- `shipping_start` 입력에 optional `commitBaseline`(기본 true)을 더했다. 코어 `createScopeProposal` 의 기본값은 **false** 로 두고 `handleStart` 가 `args.commitBaseline !== false` 로 넘긴다. 코어 기본값을 true 로 하면 `createScopeProposal` 을 직접 부르는 기존 단위·통합 테스트가 모두 의미를 잃기 때문이다.
- 실행 지점은 `createScopeProposal` 의 `withProposalLock` 진입 직후, `loadProposalContext`(저장소 분석) **전**이다. 따라서 제안의 `gitSha`/`baselineSha` 가 새 커밋이 된다. `test/integration/baseline-autocommit-flow.test.mjs` 가 승인 후 `lock.baselineSha === baselineCommit.sha` 를 확인한다.
- 원장에 `baseline.autocommitted`(sha, filesCommitted, untrackedIncluded, undo)를 남긴다. `stateWrite` 가 아니므로 상태 전이가 아니다.
- 응답은 `structuredContent.baselineCommit = { sha, filesCommitted, untrackedIncluded, undo }` 또는 `null`, 텍스트 맨 위 한 줄은 `Committed your working tree as the release baseline: <sha12> (N file(s), M new). Undo with: git reset --soft HEAD~1`.
- A.6: 자동 커밋이 일어난 경우에만 `runAcceptancePreflight`(v1.12.1)를 한 번 돌려 필수 인수 조건이 전부 `ALREADY_PASSING` 이면 `BASELINE_ALREADY_PASSING:` 진단과 되돌리기 문장을 붙인다. preflight 자체가 실패하면 try/catch 로 진단 없이 계속한다. 차단하지 않는다.

기존 `DIRTY_BASELINE` 경로는 그대로 살아 있고 `commitBaseline: false` 로 도달한다. 그 경로를 주제로 삼는 기존 테스트 4개(`test/adversarial/intent-gate-attacks`, `test/mcp/intent-gate`, `test/mcp/plain-brief`, `test/mcp/baseline-steward`)와 `scripts/autopilot-field-pilot.mjs` 의 dirty nested 레인에 `commitBaseline: false` 를 명시하고 이유를 주석으로 남겼다. 이 레인들은 "작업 트리가 바이트 단위로 그대로"를 검증하는 것이 목적이므로 자동 커밋을 끄는 것이 옳다.

### Phase B — 승인 후 커밋 기록

- `src/core/git.mjs` 에 `commitsBetween(root, baselineSha, headSha, limits)` 를 더했다. `rev-list` + 커밋당 `log -1 --format=%s` / `show --name-only`. 상한은 커밋 50개, 커밋당 경로 20개, subject 120자이며 초과분은 `truncated` / `pathsTruncated` 로 센다. baseline 을 해석할 수 없으면 던지지 않고 빈 기록으로 내려간다.
- 경로 목록에서 `.shipping/**` 는 뺀다. 하네스 자신의 런타임 상태는 목표 작업이 아니고 `analyzeScope`·`treeFingerprint` 등 다른 모든 범위 판정에서도 이미 무시되므로, 여기서만 나열하면 실제 경로가 묻힌다.
- `closeRelease` 가 receipt 에 `postLockCommits`(+ 초과 시 `postLockCommitsTruncated`)를 쓰고, 릴리스 보고서에 `- Commits after lock: N` 줄과 `## Commits after lock` 표가 생긴다.
- `releaseStatus` 에 `commitsSinceLock`(정수, LOCKED 이후 상태에서만 계산하고 그 외에는 `null`)을 더해 CLI `status` 텍스트(`Post-lock commits`)와 MCP user view 에 노출한다.
- `schemas/v1/release.schema.json` 에 optional 가산, 예제 갱신. 차단 지점은 한 곳도 없다.

### Phase C — 목표가 지목한 경로

- `src/core/goal-paths.mjs`(신규, 순수). `extractGoalPaths(goalText)` 는 계획과 달리 `{ accepted, refused }` 를 돌려준다. 절대 경로·`..`·`~` 를 조용히 "버리면" Phase D 가 요구하는 `GOAL_PATH_REFUSED` 진단을 만들 수 없기 때문이다. 버리는 규칙 자체는 계획과 같다.
- `scopePathsForGoal(goalText, scopePaths, root)` 가 글로브를 만든다. 파일의 상위 디렉터리 + `/**`, 상위가 리포 루트면 그 파일 경로 그대로, 끝이 `/` 면 그 디렉터리. 이미 include 에 덮이면 추가도 거부도 하지 않는다.
- 거부 사유: `absolute-path`, `parent-traversal`, `home-directory-path`, `over-length`, `null-byte`, `excluded-path`(`scope.paths.exclude` 매칭), `shipping-runtime`, `repository-internals`, `outside-repository`, `invalid-repository-path`.
- 적용 지점은 `buildMinimalScope` 한 곳이다. 이 함수는 제안이 `scope.paths.include` 를 만드는 곳이면서 동시에 `validateDecisionPackage` 의 허용 집합이기도 하므로, 여기에 넣으면 두 경로가 절대 어긋나지 않는다. 계획 단계(v1.12.0)의 `applyPlanStageToContract` 는 산문 `scope.include` 만 바꾸고 `scope.paths` 는 건드리지 않으므로 같은 목표 문장에 대해 동일하게 적용된다. 저장소만으로 만드는 부분을 `repositoryScopePaths` 로 뽑고, 진단용으로 같은 결과를 재계산하는 `goalScopePaths(analysis, goal)` 를 export 했다.
- `assertContainedPath` 는 `loadProposalContext` 에서 추가된 토큰마다 한 번씩 비동기로 검증한다. 순수 함수 쪽은 `path.resolve` 기반 동기 검사만 하고, 심볼릭 링크까지 푸는 검사는 fs 를 쓸 수 있는 제안 경로에서 한다. 여기서 실패하면 조용히 빼는 대신 제안 전체가 `ERR_PATH_OUTSIDE_REPO` 로 실패한다(불변식이 깨진 것이므로).
- 진단은 `GOAL_PATH_ADDED: <glob> (goal named "<token>")` / `GOAL_PATH_REFUSED: <token> (<reason>)` 로 `proposal.diagnostics` 에 들어가며, 이것이 `shipping_start` 응답의 `diagnostics` 로 승인 브리프와 함께 사람에게 보인다. `approvalBrief` 객체 자체의 필드는 늘리지 않았다 — 그 해시는 제안 정체성에 묶여 있고, 진단은 이미 같은 응답에서 승인 화면과 함께 표시되는 필드다.

### Phase D — 테스트

계약 AC-11301/AC-11302 가 지목한 6개 파일을 모두 추가했다.

| 파일 | 내용 |
|---|---|
| `test/unit/baseline-commit.test.mjs` | 깨끗한 트리 무동작, 추적 수정 커밋과 SHA, 삭제, 안전한 untracked 포함, `.gitignore`·`.shipping/` 제외, 손으로 스테이징한 파일과 `.shipping/`, 되돌리기가 트리를 그대로 복원 |
| `test/adversarial/baseline-commit-attacks.test.mjs` | `.env`/`id_rsa`/`credentials.json`/`config/auth.json`/`tokens.json`/`.ssh/`, 미리 스테이징된 `.env`, 8 MiB 초과, 200개 초과, `.gitignore` 대상, 커밋 없는 저장소, git identity 미설정. 모든 거부에서 `git status --porcelain` 과 `git diff --cached` 가 호출 전과 바이트 단위로 같고 HEAD 가 움직이지 않음을 확인 |
| `test/integration/baseline-autocommit-flow.test.mjs` | 더러운 트리 → `shipping_start` → 재스캔 없이 승인, `lock.baselineSha === baselineCommit.sha`, 원장 이벤트, `commitBaseline: false` 동치, 깨끗한 트리, `BASELINE_ALREADY_PASSING`, 자격증명 파일이면 시작 자체가 거부 |
| `test/unit/post-lock-commits.test.mjs` | lock 후 커밋 2개의 subject·경로, 커밋 없으면 빈 배열, 상한 초과 시 `truncated`/`pathsTruncated`, `commitsSinceLock`, 해석 불가 baseline |
| `test/unit/goal-scope-paths.test.mjs` | `src/index.mjs` → `src/**`, 깊은 경로, 루트 파일, 끝 `/`, 이미 덮인 경로, 중복, 최대 8개, 200자 초과, 문장 부호 제거, 일반 산문 |
| `test/adversarial/goal-scope-attacks.test.mjs` | 11종 경로 탈출·거부 목록, 거부와 추가의 공존, Windows/UNC 경로, 넓힌 글로브가 리포 밖을 매칭할 수 없음, **§1.3 시나리오 재현**(빈 `src/` 에서 목표가 `src/index.mjs` 를 지목 → 승인 → 구현 → 첫 verify 가 scope drift 없이 SHIPPABLE), 목표가 `.shipping/state.json` 과 `../../etc/passwd` 를 지목해도 범위에 들어가지 않음 |

### Phase E — 문서

`docs/MCP.md`(shipping_start 입력·출력·되돌리기, "Baseline auto-commit" / "Goal-named scope paths" / "Commits made after the lock" 절과 진단 코드 목록), `docs/HANDOVER.md`(하네스가 로컬 커밋을 만드는 유일한 지점과 그 경계, `postLockCommits` 가 증거이지 게이트가 아니라는 점), `docs/BEGINNER-QUICKSTART-KO.md`(정리하지 않고 시작해도 된다는 한 단락), `packages/shipping-plugin/skill/SKILL.md`(자동 커밋 보고 규칙, `GOAL_PATH_*` 진단 표시, post-lock 커밋은 판정이 아님 — 목록 번호도 이전부터 중복돼 있어 함께 정리), `CHANGELOG.md`.

### 표면 동결 해시 갱신

| 표면 | 이전 | 새 값 | 사유 |
|---|---|---|---|
| `tools` | `96807c58dc354de688d6fe4e66ae93dfa01b95ec03fa497bf755676f2a5a8792` | `a8038208c25805a295802e2cdcef6bec0136afe2a5ec3075b35a8e6b8137eca0` | `shipping_start` 에 optional `commitBaseline`(boolean, 기본 true) 1개 가산. 툴 9개 유지, 이름 변경·삭제 없음, `additionalProperties: false` 유지, 금지된 입력 속성명 없음 |
| `schemas` | `51ee7657dcb759ab404c150b9c6f0a9939ed074e208710542761c431e153ec0e` | `faec1160a58ad1dfcab9f613973faced4401cfe61a07752cec9a3af589c11c29` | `release.schema.json` 에 optional `postLockCommits` / `postLockCommitsTruncated` 가산, 예제 갱신. 필수 필드 추가·삭제 없음 |
| `help` | 변경 없음 | 변경 없음 | v1.13.0 은 CLI 명령·플래그를 추가하지 않는다 |

사유는 `test/stable/surface-freeze.test.mjs` 에도 이전 해시와 함께 기록했다.

### 게이트

`npm run check`(lint + typecheck + build + test, 399 tests), `lint:eslint`, `typecheck:tsc`, `test:coverage`, `security`, `verify:docs`, `release:verify`(17/17), 계약의 AC-11301 ~ AC-11305, `git diff --check`, `git status` clean — 전부 통과.

### 남은 판단거리

- 자동 커밋은 저장소 훅을 실행하지 않는다(`--no-verify`). 훅으로 포맷을 강제하는 저장소에서는 베이스라인 커밋이 훅을 거치지 않은 상태로 남는다. 분석 경로가 저장소 코드를 실행하지 않는다는 더 강한 경계를 택한 결과다.
- `commitBaseline` 기본값이 true 이므로, MCP 호스트가 `shipping_start` 를 부르는 것만으로 사용자 트리에 커밋이 생긴다. 되돌리기는 한 줄이고 응답 첫 줄에 항상 표시되지만, 이것이 v1.13.0 에서 사용자가 체감하는 가장 큰 행동 변화다.
