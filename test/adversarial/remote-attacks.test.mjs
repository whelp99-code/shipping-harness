import test from'node:test';import assert from'node:assert/strict';import path from'node:path';import{validateRemoteConfig,InternalRemoteGateway,ReplayStore,NotificationStore,RateLimiter,remoteError,signRemoteRequest}from'../../packages/internal-remote/index.mjs';import{fixture,request,FakeAdapter,actorSecret,serverSecret}from'../remote/helpers.mjs';
test('cross-project access and insufficient permission fail before adapter execution',async()=>{const f=await fixture();try{const config=validateRemoteConfig({schema:'shipping-harness/remote-config-v1',serverSecret,actors:[{id:'reader',secret:actorSecret,permissions:['read'],projects:['p1']}],projects:[{id:'p1',root:f.project},{id:'p2',root:f.root}]});const adapter=new FakeAdapter();const g=new InternalRemoteGateway({config,replayStore:new ReplayStore(path.join(f.root,'r.json')),adapter,notifications:new NotificationStore(path.join(f.root,'n.jsonl')),backupRoot:path.join(f.root,'b')});const base={schema:'shipping-harness/remote-request-v1',requestId:'x',actorId:'reader',projectId:'p2',action:'shipping/status',params:{},timestamp:new Date().toISOString(),nonce:'x'};await assert.rejects(()=>g.handle(signRemoteRequest(base,actorSecret)),/not allowed/i);const close={...base,projectId:'p1',action:'shipping/close',nonce:'y'};await assert.rejects(()=>g.handle(signRemoteRequest(close,actorSecret)),/lacks/i);assert.equal(adapter.calls.length,0);}finally{await f.cleanup();}});
test('request forgery, replay and nonce races fail closed exactly once',async()=>{const f=await fixture();try{const adapter=new FakeAdapter();const g=new InternalRemoteGateway({config:f.config,replayStore:f.replay,adapter,notifications:f.notices,backupRoot:path.join(f.root,'b')});const r=request({action:'shipping/status',nonce:'race'});const results=await Promise.allSettled([g.handle(r),g.handle(r),g.handle(r)]);assert.equal(results.filter(x=>x.status==='fulfilled').length,1);assert.equal(adapter.calls.length,1);await assert.rejects(()=>g.handle({...request({action:'shipping/status',nonce:'forged'}),signature:'0'.repeat(64)}),/signature/i);}finally{await f.cleanup();}});
test('rate and concurrency policies are bounded rather than unlimited',async()=>{const limiter=new RateLimiter(2,60000);limiter.claim('a',0);limiter.claim('a',0);assert.throws(()=>limiter.claim('a',0),/rate/i);});
test('errors redact secret, token and authorization-looking values',()=>{const e=new Error('secret=abc token:xyz authorization=Bearer123');const out=remoteError('x',e);assert.doesNotMatch(out.error.message,/abc|xyz|Bearer123/);assert.match(out.error.message,/REDACTED/);});
test('unknown or raw execution fields cannot be tunneled through nested params',async()=>{const f=await fixture();try{const{gateway:g}=await import('../remote/helpers.mjs').then(m=>m.gateway(f));for(const params of[{goal:'x',nested:{command:'bash'}},{goal:'x',environment:{TOKEN:'x'}},{goal:'x',argv:['--danger']}])await assert.rejects(()=>g.handle(request({action:'shipping/start',params,nonce:Math.random().toString()})),/forbidden/i);}finally{await f.cleanup();}});
test('approval receipt cannot cross actor, project, release, proposal or expiration boundaries',async()=>{const f=await fixture();try{const{gateway:g}=await import('../remote/helpers.mjs').then(m=>m.gateway(f));const issued=await g.handle(request({action:'approval/issue',params:{proposalId:'P',proposalHash:'a'.repeat(64),release:'0.9.0',confirm:true},nonce:'i'}));for(const p of[{proposalId:'Q',proposalHash:'a'.repeat(64),release:'0.9.0'},{proposalId:'P',proposalHash:'b'.repeat(64),release:'0.9.0'},{proposalId:'P',proposalHash:'a'.repeat(64),release:'1.0.0'}])await assert.rejects(()=>g.handle(request({action:'shipping/approve',params:p,approvalReceipt:issued.result,nonce:Math.random().toString()})),/binding/i);}finally{await f.cleanup();}});
