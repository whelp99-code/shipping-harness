import test from'node:test';import assert from'node:assert/strict';import path from'node:path';import{readFile,writeFile}from'node:fs/promises';import{createBackup,restoreBackup,validateBackup}from'../../packages/internal-remote/index.mjs';import{fixture,serverSecret}from'./helpers.mjs';
test('signed backup restores Shipping state and preserves an evidence-only OMO manifest',async()=>{const f=await fixture();try{const extra=path.join(f.root,'runtime-manifest.json');await writeFile(extra,JSON.stringify({release:'0.7.0'})+'\n');const bundlePath=path.join(f.root,'backups','one.json');const made=await createBackup({projectId:'p1',projectRoot:f.project,serverSecret,outputPath:bundlePath,extraEvidenceFiles:[{source:extra,logicalPath:'omo/runtime-manifest.json'}]});assert.ok(made.fileCount>=3);const bundle=validateBackup(JSON.parse(await readFile(bundlePath,'utf8')),{projectId:'p1',serverSecret});assert.ok(bundle.files.some(x=>x.path==='evidence/omo/runtime-manifest.json'&&x.restore===false));await writeFile(path.join(f.project,'.shipping','state.json'),JSON.stringify({state:'CLOSED',release:'BROKEN'})+'\n');const restored=await restoreBackup({projectId:'p1',projectRoot:f.project,serverSecret,bundlePath});assert.equal(restored.restored,true);assert.equal(JSON.parse(await readFile(path.join(f.project,'.shipping','state.json'),'utf8')).release,'0.9.0');}finally{await f.cleanup();}});
test('tampered bundle and active-state restore fail closed',async()=>{const f=await fixture();try{const p=path.join(f.root,'b.json');await createBackup({projectId:'p1',projectRoot:f.project,serverSecret,outputPath:p});const b=JSON.parse(await readFile(p,'utf8'));b.files[0].bytes=Buffer.from('evil').toString('base64');await writeFile(p,JSON.stringify(b));await assert.rejects(()=>restoreBackup({projectId:'p1',projectRoot:f.project,serverSecret,bundlePath:p}),/signature|integrity/i);}finally{await f.cleanup();}});
