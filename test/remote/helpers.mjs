import os from 'node:os';import path from 'node:path';import {mkdtemp,mkdir,rm,writeFile}from'node:fs/promises';import {execFileSync}from'node:child_process';import {hmac,signRemoteRequest,validateRemoteConfig,ReplayStore,NotificationStore,InternalRemoteGateway}from'../../packages/internal-remote/index.mjs';
export const actorSecret='actor-secret-abcdefghijklmnopqrstuvwxyz';export const serverSecret='server-secret-abcdefghijklmnopqrstuvwxyz-0123456789';
export async function fixture(){const root=await mkdtemp(path.join(os.tmpdir(),'shipping-remote-test-'));const project=path.join(root,'project');await mkdir(path.join(project,'.shipping','releases'),{recursive:true});await writeFile(path.join(project,'.shipping','state.json'),JSON.stringify({state:'CLOSED',release:'0.9.0'})+'\n');await writeFile(path.join(project,'.shipping','contract.yaml'),JSON.stringify({release:'0.9.0'})+'\n');await writeFile(path.join(project,'.shipping','ledger.jsonl'),'');const config=validateRemoteConfig({schema:'shipping-harness/remote-config-v1',serverSecret,actors:[{id:'owner',secret:actorSecret,permissions:['read','write','approve','control','close','admin'],projects:['p1']}],projects:[{id:'p1',root:project}],ratePerMinute:100});const replay=new ReplayStore(path.join(root,'remote','replay.json'));const notices=new NotificationStore(path.join(root,'remote','notifications.jsonl'));return{root,project,config,replay,notices,cleanup:()=>rm(root,{recursive:true,force:true})};}
export function request({action,params={},nonce=`n-${Math.random()}`,projectId='p1',actorId='owner',timestamp=new Date().toISOString(),approvalReceipt}){const unsigned={schema:'shipping-harness/remote-request-v1',requestId:`r-${Math.random()}`,actorId,projectId,action,params,timestamp,nonce,...(approvalReceipt?{approvalReceipt}: {})};return signRemoteRequest(unsigned,actorSecret);}
export class FakeAdapter{constructor(){this.calls=[];this.state='LOCKED';}async call(projectId,action,params){this.calls.push({projectId,action,params});if(action==='shipping/status')return{state:{state:this.state,release:'0.9.0'},contract:{release:'0.9.0'},issues:{counts:{BLOCKER:0}},evidenceFresh:true};if(action==='shipping/approve')return{state:{state:'LOCKED',release:'0.9.0'},proposalId:params.proposalId};if(action==='shipping/pause'){this.state='PAUSED';return{state:{state:'PAUSED',release:'0.9.0'}};}if(action==='shipping/resume'){this.state='LOCKED';return{state:{state:'LOCKED',release:'0.9.0'}};}if(action==='shipping/close'){this.state='CLOSED';return{state:{state:'CLOSED',release:'0.9.0'},contract:{release:'0.9.0'}};}return{state:{state:this.state,release:'0.9.0'},result:'ok'};}}
export function gateway(f,adapter=new FakeAdapter()){return{adapter,gateway:new InternalRemoteGateway({config:f.config,replayStore:f.replay,adapter,notifications:f.notices,backupRoot:path.join(f.root,'backups')})};}
