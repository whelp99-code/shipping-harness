import test from'node:test';import assert from'node:assert/strict';import{assertSupportedUpgrade,deprecationNotice,migrateArtifact,STABLE_SCHEMAS}from'../../packages/stable-control/index.mjs';
test('supported v0.6 through v0.9 upgrades are explicit',()=>{for(const v of['0.6.0','0.7.0','0.8.0','0.9.0'])assert.equal(assertSupportedUpgrade(v).supported,true);assert.throws(()=>assertSupportedUpgrade('0.5.0'),/Unsupported/);});
test('legacy contract migrates identifiers without changing scope or acceptance',()=>{const legacy={schema:'shipping-harness/v1',project:'p',release:'0.9.0',goal:'g',scope:{include:['x']},acceptance:[{id:'A'}]};const result=migrateArtifact(legacy);assert.equal(result.toSchema,STABLE_SCHEMAS.contract);assert.deepEqual(result.artifact.scope,legacy.scope);assert.deepEqual(result.artifact.acceptance,legacy.acceptance);assert.equal(deprecationNotice(legacy.schema).deprecated,true);});
test('unknown major schema fails closed',()=>assert.throws(()=>migrateArtifact({schema:'shipping-harness/contract-v99'}),/Unknown|schema/i));
